import { post } from "../js/formHandler";

describe('testing if the post function will return a promise or not',()=>{
    

    test('testing if the post function will return a promise or not', () => {
        expect(post).toBeDefined();
      })

})